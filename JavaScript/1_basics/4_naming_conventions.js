/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다
 * 2) 특수기호는 언더스코어와 달러를 사용할 수 있다
 * 3) 숫자로 이름을 시작할 수 없다
 * 4) 키워드는 변수명으로 사용할 수 없다 (var, let, const)
 */

let name = '이예지';
var $new = 'iphone';
const _newnew = 'new Name';

console.log(name);

/**
 * Naming Conventions
 * 
 * 1) camelCase 
 * 2) snake_case
 * 3) PascalCase
 */